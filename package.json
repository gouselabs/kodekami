{
  "name": "codekami",
  "displayName": "CodeKami",
  "description": "Turn your coding journey into an anime-inspired RPG. Offline, private, no backend.",
  "version": "0.1.0",
  "publisher": "gouselabs",
  "icon": "icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/gouselabs/kodekami.git"
  },
  "bugs": {
    "url": "https://github.com/gouselabs/kodekami/issues"
  },
  "homepage": "https://github.com/gouselabs/kodekami#readme",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other",
    "Themes"
  ],
  "keywords": [
    "anime",
    "developer",
    "gamification",
    "rpg",
    "productivity",
    "coding",
    "developer tools",
    "anime theme",
    "vs code"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "codekami.openDashboard",
        "title": "Open Dashboard",
        "category": "CodeKami"
      },
      {
        "command": "codekami.changeCharacter",
        "title": "Change Character",
        "category": "CodeKami"
      },
      {
        "command": "codekami.viewAchievements",
        "title": "View Achievements",
        "category": "CodeKami"
      },
      {
        "command": "codekami.generateDeveloperCard",
        "title": "Generate Developer Card",
        "category": "CodeKami"
      },
      {
        "command": "codekami.resetProgress",
        "title": "Reset Local Progress",
        "category": "CodeKami"
      },
      {
        "command": "codekami.viewLastSession",
        "title": "View Last Session",
        "category": "CodeKami"
      },
      {
        "command": "codekami.viewAnalytics",
        "title": "View Analytics",
        "category": "CodeKami"
      },
      {
        "command": "codekami.changeTheme",
        "title": "Change Theme",
        "category": "CodeKami"
      }
    ],
    "themes": [
      {
        "label": "CodeKami Dark",
        "uiTheme": "vs-dark",
        "path": "./themes/codekami-dark-color-theme.json"
      }
    ],
    "configuration": {
      "title": "CodeKami",
      "properties": {
        "codekami.enableNotifications": {
          "type": "boolean",
          "default": true,
          "description": "Show notifications for streak milestones. Level-up and achievement reactions are controlled by the reaction settings below."
        },
        "codekami.sessionStartXp": {
          "type": "number",
          "default": 5,
          "minimum": 0,
          "description": "XP awarded once when a coding session starts."
        },
        "codekami.fileSaveXp": {
          "type": "number",
          "default": 2,
          "minimum": 0,
          "description": "XP awarded when a file is saved (subject to the cooldown below)."
        },
        "codekami.fileSaveCooldownSeconds": {
          "type": "number",
          "default": 60,
          "minimum": 1,
          "description": "Minimum seconds between file-save XP rewards, per file."
        },
        "codekami.showReactions": {
          "type": "boolean",
          "default": true,
          "description": "Show short anime-style reactions when you level up or unlock an achievement."
        },
        "codekami.reactionMode": {
          "type": "string",
          "enum": ["notification", "statusBar", "both"],
          "default": "both",
          "description": "How reactions are shown: a notification popup, a transient status bar message, or both."
        },
        "codekami.reactionCooldownSeconds": {
          "type": "number",
          "default": 45,
          "minimum": 10,
          "description": "Minimum seconds between normal reactions. Level-up and achievement reactions always bypass this cooldown."
        },
        "codekami.sessionInactivityMinutes": {
          "type": "number",
          "default": 30,
          "minimum": 1,
          "description": "Minutes of inactivity before a coding session is considered ended."
        },
        "codekami.showSessionSummary": {
          "type": "boolean",
          "default": true,
          "description": "Show a notification with a session summary when a coding session ends."
        },
        "codekami.showCompanion": {
          "type": "boolean",
          "default": true,
          "description": "Show your CodeKami companion in the status bar. It reacts to level-ups, achievements, builds, tests, commits, and long sessions."
        },
        "codekami.enableSounds": {
          "type": "boolean",
          "default": false,
          "description": "Play short sound effects for level-ups, achievements, builds, tests, commits, sessions, and streak milestones. Off by default."
        },
        "codekami.soundVolume": {
          "type": "number",
          "default": 70,
          "minimum": 0,
          "maximum": 100,
          "description": "Sound effect volume, as a percentage."
        },
        "codekami.showDailyMotivation": {
          "type": "boolean",
          "default": true,
          "description": "Show a short, original daily motivational message on the dashboard. The same message shows all day, chosen deterministically — no server or randomness involved."
        },
        "codekami.debugMode": {
          "type": "boolean",
          "default": false,
          "description": "Log CodeKami activity (XP, level-ups, achievements, streaks, sessions) to the 'CodeKami' output channel. No source code or file names are logged."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "npm run check-types && node esbuild.js",
    "watch": "node esbuild.js --watch",
    "check-types": "tsc --noEmit",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "lint": "eslint src --ext ts",
    "pretest": "tsc -p tsconfig.json",
    "test": "node --test \"out/test/**/*.test.js\"",
    "vsix": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "@vscode/vsce": "^2.24.0",
    "esbuild": "^0.20.0",
    "eslint": "^8.57.0",
    "typescript": "^5.4.0"
  }
}
